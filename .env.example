# Shared Supabase project (same one AdsLens / DispatchLens use).
# Service role key is SERVER-ONLY — never prefix with NEXT_PUBLIC.
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
